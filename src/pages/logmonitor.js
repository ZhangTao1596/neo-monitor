import React, {Component} from "react";
import LogBox from "../unit/logbox";

export default class MainPage extends Component {
    render() {
        return (
        <div style={{height: "95%"}}>
            <LogBox />
        </div>)
    }
}